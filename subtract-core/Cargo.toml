[package]
name = "wasapi-subtract-core"
version = "0.1.0"
edition = "2021"
rust-version = "1.82.0"
description = "Portable timeline/alignment/subtraction core for endpoint-minus-own-process capture (no Windows dependencies, testable anywhere)."
license = "MIT"
publish = false

[dependencies]
